%h1= @record.title
%p= @record.content

= link_to "BACK", root_path
= link_to "EDIT", edit_record_path(@record)
= link_to "Delete", record_path(@record), method: :delete, data: { confirm: "Are you sure you want to delete this record?" }